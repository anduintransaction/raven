root_dir: .
build:
  - name: phusion/baseimage
    tag: 0.9.22
    from: provided

  - name: anduin/raven
    tag: {{.ravenTag}}
    from: .
credentials:
  - name: dockerhub
    username: ${DOCKERHUB_USERNAME}
    password: ${DOCKERHUB_PASSWORD}
